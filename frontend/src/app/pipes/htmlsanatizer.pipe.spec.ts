import { HtmlsanatizerPipe } from './htmlsanatizer.pipe';

describe('HtmlsanatizerPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlsanatizerPipe();
    expect(pipe).toBeTruthy();
  });
});
